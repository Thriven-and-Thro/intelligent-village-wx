<!--components/content/content.wxml-->
<view class="article-detail">
  <view class="detail-text">
    <rich-text space="nbsp" nodes="{{content}}" wx:if="{{rich}}"></rich-text>
    <text wx:if="{{!rich}}">{{content}}</text>
  </view>
  <view class="date">{{date}}</view>
</view>